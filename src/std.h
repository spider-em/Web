/* standard header file */

#ifndef STDH
#define STDH

#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>
#include <time.h>
#include <stdlib.h>
#include <limits.h>
#include <float.h>

#define  TRUE  1
#define  FALSE 0
#define  MYMAX(A,B)  ((A) > (B) ? (A) : (B))
#define  MYMIN(A,B)  ((A) < (B) ? (A) : (B))
#define  FLTZER 10e-10

#endif
